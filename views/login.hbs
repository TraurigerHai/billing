<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link type="image/x-icon" href="/img/favicon.svg" rel="shortcut icon">
    <link type="Image/x-icon" href="/img/favicon.svg" rel="icon">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap" rel="stylesheet">
    <title>Вход в систему</title>
</head>
<body>
    <div class="main-container">
        <main class="content login-content">
            <div class="login-section">
                <div class="section-header">
                    <div class="header-icon">👤</div>
                    <h2>Вход в систему</h2>
                </div>
                <div class="form-card">
                    <form action="/login" method="POST" class="notification-form">
                        <div class="form-field">
                            <label class="field-label">Логин</label>
                            <input type="text" name="login" required class="field-input" placeholder="Введите логин">
                        </div>
                        <div class="form-field">
                            <label class="field-label">Пароль</label>
                            <input type="password" name="password" required class="field-input" placeholder="Введите пароль">
                        </div>
                        <button type="submit" class="action-button">Войти</button>
                        {{#if error}}
                        <div class="error-message">{{error}}</div>
                        {{/if}}
                    </form>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
